<button
    data-ui-role="toggle"
    data-ui-fragment="blocks.ext.toggle"
    data-controller="symfony--ux-blocks-extended--toggle"
    type="button"
    aria-pressed="false"
    data-action="click->symfony--ux-blocks-extended--toggle#toggle"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</button>
