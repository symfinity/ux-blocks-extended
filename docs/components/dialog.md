# dialog

**Role:** `dialog`  
**Fragment id:** `blocks.dialog`  
**Twig:** `Dialog`

Native `<dialog>` overlay — no default package Stimulus. Open with `showModal()` / `close()` from host app or documented native `commandfor` pattern.

Prose alias **modal** maps to this role (`blocks.dialog`).
