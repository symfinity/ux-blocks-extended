# empty

**Role:** `empty`  
**Fragment id:** `blocks.ext.empty`

**Props:** `title`, `description`, `appearance` (`outline` default | `soft` | `plain`).

**Media region:** universal `Media` component — `variant` `default` | `icon` (shadcn EmptyMedia parity); `icon` prop for glyph slot.
